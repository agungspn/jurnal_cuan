rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Screenshot hanya bisa diakses oleh pemiliknya
    match /screenshots/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
